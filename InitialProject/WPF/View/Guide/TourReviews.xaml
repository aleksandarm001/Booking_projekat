<Window x:Class="InitialProject.WPF.View.Guide.TourReviews"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:InitialProject.WPF.View.Guide"
        mc:Ignorable="d"
        Title="TourReviews" Height="516" Width="314">
    <Grid>

        <ComboBox x:Name="Cmbx" ItemsSource="{Binding Tours}" SelectedValue="{Binding Path=SelectedTour, UpdateSourceTrigger=PropertyChanged}" HorizontalAlignment="Left" VerticalAlignment="Top" Width="216" Margin="10,30,0,0"/>
        <Button Content="Pokazi" Click="ShowStatisticButton" HorizontalAlignment="Left" Margin="231,28,0,0" VerticalAlignment="Top" Width="59" Height="25" RenderTransformOrigin="0.484,-0.454"/>

        <DataGrid Name ="ValidComments" Grid.Row="0" Margin="10,75,10,250" 
                  ItemsSource="{Binding Path = Users, UpdateSourceTrigger=PropertyChanged}"
                  SelectedItem="{Binding Path= SelectedTourAttendance, UpdateSourceTrigger=PropertyChanged}"
                  IsReadOnly="True"  SelectionMode="Single" AutoGenerateColumns="false"
                  Visibility ="Visible">

            <DataGrid.Columns>
                <DataGridTextColumn Header="Id" Width="30" Binding="{Binding UserId}"></DataGridTextColumn>
                <DataGridTextColumn Header="Tour Point" Width="100" Binding="{Binding TourId}"></DataGridTextColumn>
                <DataGridTextColumn Header="Comment" Width="150" Binding="{Binding Comment}"></DataGridTextColumn>
            </DataGrid.Columns>

        </DataGrid>
        <DataGrid x:Name ="NotValidComment" Grid.Row="0" Margin="10,269,10,63" 
            ItemsSource="{Binding Users, UpdateSourceTrigger=PropertyChanged}"
            SelectedItem="{Binding SelectedTourAttendance, UpdateSourceTrigger=PropertyChanged}"
            IsReadOnly="True" SelectionMode="Single" AutoGenerateColumns="false"
            Visibility ="Visible">

            <DataGrid.Columns>
                <DataGridTextColumn Header="Id" Width="30" Binding="{Binding UserId}"></DataGridTextColumn>
                <DataGridTextColumn Header="Tour Point" Width="100" Binding="{Binding TourId}"></DataGridTextColumn>
                <DataGridTextColumn Header="Comment" Width="150" Binding="{Binding Comment}"></DataGridTextColumn>
            </DataGrid.Columns>

        </DataGrid>
        <Button Content="Obrisi komentar" Click="DeleteFakeCommentButton" HorizontalAlignment="Center" Margin="0,455,0,0" VerticalAlignment="Top" Width="108" Height="25" RenderTransformOrigin="0.484,-0.454"/>

    </Grid>
</Window>
